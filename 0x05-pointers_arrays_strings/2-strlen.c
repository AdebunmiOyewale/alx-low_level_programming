#include "main.h"

/**
 * _strlen - that returns the length of a string.
 * @s: the character
 *
 * Return: lenght
 */
int _strlen(char *s)
{
	int lenght = 0;


	while (*s != '\0')
	{
		len++;
		s++;
	}
	return (len);
}
